{
  "name": "growth99-figma-plugin",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "clean": "rm -rf dist",
    "dev": "npm run build && npm run watch",
    "backend": "PYTHONPATH=. python -m backend.main",
    "backend:install": "cd backend && pip install -r requirements.txt",
    "start": "npm run backend"
  },
  "devDependencies": {
    "esbuild": "^0.21.5",
    "typescript": "^5.9.2"
  }
}
